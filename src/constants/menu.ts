export enum MenuMode {
    Dropdown = 'dropdown',
    Modal = 'modal',
    Router = 'router'
}

export enum InputType {
    INPUT = 'input',
    PASSWORD = 'password',
    SELECT = 'select',
    INPUT_NUMBER = 'input_number'
}